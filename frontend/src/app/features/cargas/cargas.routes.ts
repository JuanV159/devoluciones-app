import { Routes } from '@angular/router';
import { CargaUploadComponent } from './carga-upload/carga-upload.component';

export const CARGAS_ROUTES: Routes = [
  { path: '', component: CargaUploadComponent },
];
